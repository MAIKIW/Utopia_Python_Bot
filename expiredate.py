
import sys # <--- ใส่ตรงimport

from datetime import datetime  # <--- ต้องมีบรรทัดนี้ด้วย ไม่งั้นโค้ดจะ error

# <--- ใส่บรรทัด15

# --- ส่วนป้องกันวันหมดอายุ (วางตรงนี้เลย!) ---
expire_date = datetime(2026, 2, 1) # ตั้งวันหมดอายุ (ปี, เดือน, วัน)
if datetime.now() > expire_date:
    print("❌ หมดเวลาทดสอบแล้วครับ! โปรดติดต่อ Anapat")
    time.sleep(10)
    sys.exit() # คำสั่งนี้จะปิดโปรแกรมทันที ไม่ให้โค้ดด้านล่างทำงานต่อ
# ----------------------------------------